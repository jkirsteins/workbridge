//! Test suite for the `mcp` module.
//!
//! Split into sub-files by topic so every file stays well under the
//! 700-line ceiling enforced by `hooks/budget-check.sh`.

mod dispatch;
mod integration;
